#include <stdio.h>
#include <stdlib.h>


int main(int argc, char *argv[])
{

    FILE *fin;
    int ia;
    int ja;

    fin = fopen("A.txt", "r");
    
    int i[];
    for(int x = 0; x < ia; x++)
    {
        fscanf(fin, "%f", &i)
    }
    
}